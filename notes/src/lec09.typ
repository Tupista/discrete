== $section$ Group Homomorphism
#linebreak()

*Definition* $quad$ Group homomorphism, kernel, image.

*Proposition* $quad$ For homomorphism $f:G->H, ker f lt.tri G.$

*Definition* $quad$ Isomorphism.

*Theorem* $quad$ Homomorphism $f:G\/ker f->im f$ is an isomorphism.

*Theorem* $quad$ Let $H,K subset G, K lt.tri G.$ Then $ (H K)/K tilde.eq H/(H inter K) $

The direct result is (using Lagrange's Theorem) $ abs(H K)=(abs(H)abs(K))/(abs(H inter K)) $

*Theorem* $quad$ Let $H lt.tri K lt.tri G,$ then $ 1) quad K\/H lt.tri G\/H, quad 2) quad G\/K tilde.eq (G\/H)/(K\/H). $

*Proof* $quad$ Consider $f:G\/H->G\/K, g H|->g K.$ We have $ker f=K\/H.$

*Definition* $quad B_n (FF)$ upper-trianglar, $N_n (FF)$ with diagonal $=1.$ 

*Proposition* $quad N_n lt.tri B_n lt.tri "GL"_n.$

*Proof* $quad$ First by $phi:B_n (FF)->(FF^times)^n,$ second by $det:"GL"_n (FF)->FF.$

*Definition* $quad$ Polynomial ring $FF[x]$, simple groups (no normal subgroups except from ${e}$ and itself).

If $G$ is finite and not normal, then exists $ G=H_0 gt.tri H_1 gt.tri dots gt.tri H_n={e} $ where $H_i\/H_(i+1)$ is simple.

== $section$ Abelian Groups (that are not cyclic)
#linebreak()

*Proposition* $quad$ Cyclic group $G$ satisfies $(G tilde.eq ZZ or G tilde.eq ZZ\/p ZZ).$

*Theorem (CRT)* $quad ZZ\/p_1 p_2 dots p_n ZZ tilde.eq (ZZ\/p_1 ZZ) times dots times (ZZ\/p_n ZZ), quad p_1,p_2,dots,p_n$ distinct.

*Theorem* $quad$ For any finitely generated Abelian group $G,$ $ G tilde.eq ZZ^n times ZZ_n_1 times dots times ZZ_n_t. $